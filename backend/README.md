# Backend

This directory is reserved for Kotlin/JVM Spring Boot services and shared libraries.

No service modules have been created yet. Each future service will own its application code, Liquibase migrations, tests, and Protocol Buffer contracts.
