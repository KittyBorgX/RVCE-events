# Frontend

The frontend uses TypeScript, React, and Next.js.

No user-facing pages or BFF endpoints are implemented yet. The `src/bff` directory reserves the server-side BFF boundary described in the system design.
